import { WeekDesktop, WeekMobile } from '../WeekSlots'
import { HeaderDesktop, HeaderMobile } from '../WeekHeaders'

export const WeekSlots = {
  desktop: WeekDesktop,
  mobile: WeekMobile,
}

export const WeekHeaders = {
  desktop: HeaderDesktop,
  mobile: HeaderMobile,
}
